# recipe_app
Recipe generator application that takes user-entered recipes and runs them through a reducer function using Pint for Flask, created condensed shopping list for users. Application uses a React frontend, Flask/Flask SQLAlchemy for backend APIs and data models
